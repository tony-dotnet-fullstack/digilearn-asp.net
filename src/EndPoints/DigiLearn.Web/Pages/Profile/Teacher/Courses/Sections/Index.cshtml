@page "{courseId}"
@model Tony.Web.Pages.Profile.Teacher.Courses.Sections.IndexModel
@{
    ViewData["Title"] = "manage season course" + $"`{Model.Course.Title}`";
    Layout = "Shared/_ProfileLayout";
    var counter = 1;

}
<div class="row ">
    <a asp-page="Add" asp-route-courseId="@Model.Course.Id" class="btn btn-save mr-2 mb-4 float-right">افزودن فصل new</a>

    <div class="col-12 info shadow pl-0 pr-0">
        <p class="title">
            <i class="fa fa-paper-plane"></i>
            @ViewData["Title"]
        </p>
        <div class="info-body ">
            <div id="accordion">
                @foreach (var item in Model.Course.Sections.OrderBy(d => d.DisplayOrder))
                {
                    <div class="card">
                        <div class="card-header" id="h_@item.Id">
                            <h5 class="mb-0">
                                <button class="btn btn-link " data-toggle="collapse" data-target="#a_@item.Id" aria-expanded="true" aria-controls="a_@item.Id">
                                    @item.Title
                                </button>
                            </h5>
                        </div>

                        <div id="a_@item.Id" class="collapse " aria-labelledby="h_@item.Id" data-parent="#accordion">
                            <div class="card-body">
                                <a asp-page="Episodes/Add" asp-route-courseId="@Model.Course.Id" asp-route-sectionId="@item.Id" class="btn btn-save mr-2 mb-4 float-right">افزودن قسمت new</a>

                                <table class="table table-bordered">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Title</th>
                                            <th>Time</th>
                                            <th>Status</th>
                                            <th>operation</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var episode in item.Episodes)
                                        {
                                            <tr>
                                                <td>@counter</td>
                                                <td>@episode.Title</td>
                                                <td>@episode.TimeSpan</td>
                                                <td>
                                                    @if (episode.IsActive)
                                                    {
                                                        <label>Active</label>
                                                    }
                                                    else
                                                    {
                                                        <label>disable Active</label>

                                                    }
                                                </td>
                                                <td></td>
                                            </tr>
                                            counter += 1;
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                }

            </div>
        </div>
    </div>
</div>
