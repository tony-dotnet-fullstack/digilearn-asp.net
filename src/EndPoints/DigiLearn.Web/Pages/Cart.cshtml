@page "{handler?}"
@model Tony.Web.Pages.CartModel
@{
    ViewData["title"] = "Carts";
    Layout = "Shared/_ProfileLayout";
}
<div class="row ">


    @if (Model.Order == null || Model.Order.OrderItems.Count == 0)
    {
        <div class="col-12 null-basket text-center">
            <img src="images/basket.png">
            <p>your carts empty</p>
        </div>
    }
    else
    {
        <div class="alert-warning alert col-12 ">
            <i class="fa fa-warning"></i>
            
            
        </div>
        <div class="col-12  pl-0 pr-0">
            @foreach (var item in Model.Order.OrderItems)
            {
                <div class="course">
                    <i onclick="deleteItem('@Url.Page("Cart","Delete",new {id=item.Id})','')" class="fa fa-trash"></i>
                    <a href="#">
                        <h6>@item.CourseTitle</h6>
                    </a>
                    <p>teacher : @item.TeacherFullName</p>
                    <span class="price"> @item.Price.ToString("#,0") $</span>
                </div>
            }


            <div class="utils row  custom-shadow" style="margin-top: 12px !important;">
                <div class="col-12 col-lg-6  p-2">
                    <form class="discount p-2">
                        <input type="text" class="form-control" placeholder="discount code">
                        <button type="submit" class="btn btn-success">اعمال</button>
                    </form>
                </div>
                <div class="col-12 col-lg-6  pl-0">
                    <form method="post">
                        <div class="order-info  p-2">
                            <div class="info pl-2">
                                <p>total price: : <span class="pull-left text-success">@Model.Order.TotalPrice.ToString("#,0") $</span></p>
                                <p> discount : <span class="pull-left text-success">0 $</span></p>
                                <p> pay: <span class="pull-left text-success">@Model.Order.TotalPrice.ToString("#,0") $</span></p>
                            </div>
                        </div>
                        <hr>
                        <button type="submit" class="btn btn-success btn-block">payment</button>
                    </form>

                </div>
            </div>
        </div>
    }
</div>
