@page
@using Common.Application.DateUtil
@model Tony.Web.Areas.Admin.Pages.Users.IndexModel
@{
    ViewData["title"] = "manage users";
    ViewData["selected"] = "users";
}

<form class="card mb-2">
    <div class="card-header mb-0 pb-0 d-flex justify-content-between">
        <h5>Filter</h5>
        <div class="">
            <a asp-page="Index" class="btn btn-sm btn-danger">delet filter</a>
            <button type="submit" class="btn btn-success btn-sm">اdo filter</button>
        </div>
    </div>
    <div class="card-body  mb-0">
        <div class="row">
            <div class="col-md-6">
                <label>Email</label>
                <input type="text" class="form-control" asp-for="FilterParams.Email" />
            </div>
            <div class="col-md-6">
                <label>mobile</label>
                <input type="text" class="form-control" asp-for="FilterParams.PhoneNumber" />
            </div>
            <div class="col-md-6">
                <label>from</label>
                <fieldset class="form-group position-relative has-icon-left">
                    <input type="text" autocomplete="off" value="@Model.FilterParams.StartDate.ToPersianDate()" name="stDate" class="form-control shamsi-datepicker" placeholder="select date">
                    <div class="form-control-position">
                        <i class="bx bx-calendar"></i>
                    </div>
                </fieldset>
            </div>
            <div class="col-md-6">
                <label>to Date</label>
                <fieldset class="form-group position-relative has-icon-left">
                    <input type="text" autocomplete="off" value="@Model.FilterParams.EndDate.ToPersianDate()" name="enDate" class="form-control shamsi-datepicker" placeholder="select date">
                    <div class="form-control-position">
                        <i class="bx bx-calendar"></i>
                    </div>
                </fieldset>
            </div>
        </div>
    </div>
</form>
<div class="card ">
    <div class="table-responsive card-body p-0 mb-0">
        <table class="table table-bordered table-hover mb-0">
            <thead>
                <tr>
                    <th>Name and Family</th>
                    <th>Email</th>
                    <th>telephone numbers</th>
                    <th>Register date name</th>
                    <th>operation</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.FilterResult.Data)
                {
                    <tr>
                        <td>@item.Name @item.Family</td>
                        <td>@item.Email</td>
                        <td>@item.PhoneNumber</td>
                        <td>@item.CreationDate.ToPersianDateTime()</td>
                        <td>
                            <a asp-page="Show" asp-route-id="@item.Id" class="btn btn-sm btn-success">show</a>
                            <a asp-page="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<partial name="_Pagination" model="Model.FilterResult" />

