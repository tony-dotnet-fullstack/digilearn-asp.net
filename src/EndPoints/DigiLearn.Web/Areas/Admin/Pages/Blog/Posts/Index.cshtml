@page "{handler?}"
@model Tony.Web.Areas.Admin.Pages.Blog.Posts.IndexModel
@{
    ViewData["title"] = "Blog managment";
    ViewData["selected"] = "blog_posts";
}
<form class="card mb-2">
    <div class="card-header mb-0 pb-0 d-flex justify-content-between">
        <h5>Filter</h5>
        <div class="">
            <a asp-page="Index" class="btn btn-sm btn-danger">Delete filter</a>
            <button type="submit" class="btn btn-success btn-sm">filter done</button>
        </div>
    </div>
    <div class="card-body  mb-0">
        <div class="row">
            <div class="col-md-6">
                <label>search:</label>
                <input type="text" asp-for="FilterParams.Search" class="form-control" placeholder="enter your search" />
            </div>
            <div class="col-md-6">
                <label>branch</label>
                <select class="form-control select2" asp-for="FilterParams.CategorySlug">
                    <option value="">select </option>
                    @foreach (var item in Model.Categories)
                    {
                        <option value="@item.Slug">@item.Title</option>
                    }
                </select>
            </div>
        </div>
    </div>
</form>

<button data-toggle="modal" data-target="#addModal" class="btn btn-success mb-1">add new</button>
<div class="card ">
    <div class="table-responsive card-body p-0 mb-0">
        <table class="table table-bordered table-hover mb-0">
            <thead>
                <tr>
                    <th>title</th>
                    <th>slug</th>
                    <th>autuor</th>
                    <th>branch</th>
                    <th>viwer</th>
                    <th>operation</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.FilterResult.Data)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Slug</td>
                        <td>@item.OwnerName</td>
                        <td>@item.Category.Title</td>
                        <td>@item.Visit</td>
                        <td>
                            <delete-item url="@Url.Page("Index","Delete",new {id=item.Id})">Delete</delete-item>
                            <open-modal is-bootstrap4="true" url="@Url.Page("Index","ShowEditPage",new {id=item.Id})" class="btn btn-sm btn-primary">edit</open-modal>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <partial name="_Pagination" model="Model.FilterResult" />
</div>


<div class="modal fade" id="addModal" tabindex="-1" aria-labelledby="editAddressModalLabel"
     aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="editAddressModalLabel">add</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form class="row" enctype="multipart/form-data" method="post">
                    <div class="col-md-6">
                        @Html.EditorFor(f => f.Title)
                    </div>
                    <div class="col-md-6">
                        @Html.EditorFor(f => f.Slug)
                    </div>
                    <div class="col-md-6">
                        @Html.EditorFor(f => f.OwnerName)
                    </div>
                    <div class="col-md-6">
                        <label asp-for="CategoryId"></label>
                        <select class="form-control select2" asp-for="CategoryId" style="width: 100%">
                            <option>انتخاب دسته بندی</option>
                            @foreach (var item in Model.Categories)
                            {
                                <option value="@item.Id">@item.Title</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-12">
                        @Html.EditorFor(f => f.ImageFile)
                    </div>
                    <div class="col-md-12">
                        @Html.EditorFor(f => f.Description)
                    </div>
                    <div class="col-md-6"></div>
                    <div class="col-md-12 d-flex mt-2 justify-content-end">
                        <submit />
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>